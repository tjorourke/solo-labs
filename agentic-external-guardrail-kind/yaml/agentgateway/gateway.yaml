apiVersion: gateway.networking.k8s.io/v1
kind: Gateway
metadata:
  name: extguard-gateway
  namespace: agentgateway-system
spec:
  gatewayClassName: enterprise-agentgateway
  listeners:
    - name: http
      protocol: HTTP
      port: 80
      allowedRoutes:
        namespaces:
          from: All
