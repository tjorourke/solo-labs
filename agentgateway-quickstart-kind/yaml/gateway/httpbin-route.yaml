# The demo namespace + a sample app + a Gateway (enterprise-agentgateway class) + an HTTPRoute.
# One command creates everything:  kubectl apply -f yaml/gateway/httpbin-route.yaml
apiVersion: v1
kind: Namespace
metadata: { name: demo }
---
apiVersion: apps/v1
kind: Deployment
metadata: { name: httpbin, namespace: demo, labels: { app: httpbin } }
spec:
  replicas: 1
  selector: { matchLabels: { app: httpbin } }
  template:
    metadata: { labels: { app: httpbin } }
    spec:
      containers:
        - name: httpbin
          image: mccutchen/go-httpbin:v2.15.0
          ports: [{ containerPort: 8080 }]
---
apiVersion: v1
kind: Service
metadata: { name: httpbin, namespace: demo }
spec:
  selector: { app: httpbin }
  ports: [{ port: 8000, targetPort: 8080 }]
---
apiVersion: gateway.networking.k8s.io/v1
kind: Gateway
metadata: { name: agw, namespace: demo }
spec:
  gatewayClassName: enterprise-agentgateway
  listeners:
    - name: http
      protocol: HTTP
      port: 80
      allowedRoutes: { namespaces: { from: Same } }
---
apiVersion: gateway.networking.k8s.io/v1
kind: HTTPRoute
metadata: { name: httpbin, namespace: demo }
spec:
  parentRefs: [{ name: agw }]
  rules:
    - backendRefs: [{ name: httpbin, port: 8000 }]
