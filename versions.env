# versions.env — GENERATED from versions.json by scripts/gen-versions-env.sh.
# DO NOT EDIT. Change versions.json and re-run the generator (pre-commit does
# this for you). Sourced by lab harnesses so every lab pins one product matrix.
# Source matrix updated: 2026-09-06
#
# Every var uses ${VAR:-value} so a runtime env override still wins.

# ── Solo products ─────────────────────────────────────────────────────────────
export AGW_ENT_VERSION="${AGW_ENT_VERSION:-v2.3.7}"        # Solo Enterprise for agentgateway
export AGW_OSS_VERSION="${AGW_OSS_VERSION:-v1.5.0}"          # OSS upstream agentgateway (cr.agentgateway.dev)
export AGW_CALVER_VERSION="${AGW_CALVER_VERSION:-v2026.8.2}"  # AGW CalVer line (code-mode JS sandbox etc.)
export KGW_VERSION="${KGW_VERSION:-2.3.3}"                      # Solo Enterprise for kgateway
export SOLO_ISTIO_VERSION="${SOLO_ISTIO_VERSION:-1.30.4-solo}"     # Solo Istio distro (image tag = strip -solo)
export SOLO_MGMT_VERSION="${SOLO_MGMT_VERSION:-2.14.0}"        # Solo Enterprise for Istio mgmt plane
export GLOO_OPERATOR_VERSION="${GLOO_OPERATOR_VERSION:-0.5.2}"
export SOLO_ENT_MGMT_VERSION="${SOLO_ENT_MGMT_VERSION:-0.5.6}" # Solo Enterprise management chart (UI + ClickHouse)
export KAGENT_ENT_VERSION="${KAGENT_ENT_VERSION:-0.5.6}" # Solo Enterprise for kagent
export KAGENT_OSS_VERSION="${KAGENT_OSS_VERSION:-0.10.0}" # kagent OSS (ghcr.io/kagent-dev), a separate line
export AR_OSS_VERSION="${AR_OSS_VERSION:-0.4.0}"             # agentregistry OSS
export AR_ENT_VERSION="${AR_ENT_VERSION:-2026.8.0}"            # Solo Enterprise for agentregistry

# ── Infra / ecosystem ─────────────────────────────────────────────────────────
export GATEWAY_API_VERSION="${GATEWAY_API_VERSION:-v1.5.1}"
export METALLB_VERSION="${METALLB_VERSION:-v0.16.0}"
export KEYCLOAK_VERSION="${KEYCLOAK_VERSION:-26.0}"
export K8S_VERSION="${K8S_VERSION:-1.35}"
export SEMANTIC_ROUTER_VERSION="${SEMANTIC_ROUTER_VERSION:-0.2.0}"
export VLLM_SIM_IMAGE="${VLLM_SIM_IMAGE:-ghcr.io/llm-d/llm-d-inference-sim:v0.5.0}"
export GIE_VERSION="${GIE_VERSION:-v1.4.0}"                      # Gateway API Inference Extension (InferencePool/EPP)
