apiVersion: v1
kind: Service
metadata:
  name: curation-inspector-ui
  namespace: tool-curation
  labels: { app: curation-inspector-ui }
spec:
  selector: { app: curation-inspector-ui }
  ports:
    - name: http
      port: 8080
      targetPort: 8080
      appProtocol: http
