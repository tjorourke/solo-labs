# Generated by scripts/render-native-routing.py. Edit native/decision.cel or opa/routing-data.json.
apiVersion: enterpriseagentgateway.solo.io/v1alpha1
kind: EnterpriseAgentgatewayPolicy
metadata:
  name: routing-outcome
  namespace: agentgateway-system
spec:
  targetRefs:
  - group: gateway.networking.k8s.io
    kind: Gateway
    name: decision-gateway
  traffic:
    extAuth:
      backendRef:
        name: routing-audit
        namespace: agentgateway-system
        port: 9191
      grpc: {}
      forwardBody:
        maxSize: 8Mi
      failureMode: FailClosed
    directResponse:
      conditional:
      - condition: '!has(metadata.routing) || !(metadata.routing.status in [200, 422])'
        policy:
          status: 403
          headers:
          - name: content-type
            value: application/json
          body: '{"error":{"type":"forbidden","message":"no suitable permitted backend for this task"}}'
      - condition: metadata.routing.status == 422
        policy:
          status: 422
          headers:
          - name: content-type
            value: application/json
          bodyExpression: 'toJson({"error": {"type": "blocked", "message": metadata.routing.error, "param":
            null, "code": null}})'
    transformation:
      request:
        remove:
        - x-agw-routing-decision
