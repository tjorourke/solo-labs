# Lab egress allowlist. This intentionally permits the whole kagent namespace,
# plus DNS, istiod and the telemetry collector. It is not a gateway-only rule.
# Other matching NetworkPolicies can add permissions. Narrow destination selectors
# and ports before using this pattern in a shared production namespace, and review
# indirect access through every allowed service. Enforcement depends on the CNI.
#
# Two rules are there because of the mesh. ztunnel fetches and renews a pod's certificate
# through that pod's own network namespace, so istiod (15012) must be reachable or the
# pod stops accepting mesh connections when the certificate is next renewed. And a
# connection to another mesh workload leaves the pod on the HBONE port, 15008, not on
# the port the application dialled, so the collector rule allows both.
apiVersion: networking.k8s.io/v1
kind: NetworkPolicy
metadata:
  name: contained-agents-egress
  namespace: kagent
spec:
  podSelector:
    matchExpressions:
    - key: app.kubernetes.io/name
      operator: In
      values: [sre-contained, sre-caller, sre-other]
  policyTypes: [Egress]
  egress:
  - to:
    - namespaceSelector:
        matchLabels:
          kubernetes.io/metadata.name: kube-system
    ports:
    - { protocol: UDP, port: 53 }
    - { protocol: TCP, port: 53 }
  - to:
    - namespaceSelector:
        matchLabels:
          kubernetes.io/metadata.name: kagent
  - to:
    - namespaceSelector:
        matchLabels:
          kubernetes.io/metadata.name: istio-system
    ports:
    - { protocol: TCP, port: 15012 }
  - to:
    - namespaceSelector:
        matchLabels:
          kubernetes.io/metadata.name: solo-cost
    ports:
    - { protocol: TCP, port: 4317 }
    - { protocol: TCP, port: 15008 }
