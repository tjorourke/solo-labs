# The model, behind a waypoint of its own.
#
# Configure the agent's model calls to use a gateway.
# The agent is given http://sre-model.kagent.svc.cluster.local as the Anthropic base URL;
# the Service has no pods, the label sends every call to the waypoint, and the waypoint
# originates TLS to api.anthropic.com. The egress policy in 70-egress-policy.yaml
# restricts direct external connections but also permits infrastructure traffic.
#
# HTTP/1.1 is pinned on the upstream hop. A reset HTTP/2 stream in the middle of a tool
# call leaves a tool_use with no tool_result, and the next model request is rejected.
apiVersion: v1
kind: Service
metadata:
  name: sre-model
  namespace: kagent
  labels:
    istio.io/use-waypoint: sre-model-waypoint
spec:
  ports:
  - name: http
    port: 80
    targetPort: 80
---
apiVersion: gateway.networking.k8s.io/v1
kind: Gateway
metadata:
  name: sre-model-waypoint
  namespace: kagent
spec:
  gatewayClassName: enterprise-agentgateway-waypoint
  listeners:
  - name: mesh
    port: 15008
    protocol: HBONE
    allowedRoutes:
      namespaces:
        from: Same
---
apiVersion: enterpriseagentgateway.solo.io/v1alpha1
kind: EnterpriseAgentgatewayBackend
metadata:
  name: sre-model
  namespace: kagent
spec:
  static:
    host: api.anthropic.com
    port: 443
  policies:
    tls:
      sni: api.anthropic.com
    http:
      version: HTTP1
---
apiVersion: gateway.networking.k8s.io/v1
kind: HTTPRoute
metadata:
  name: sre-model
  namespace: kagent
spec:
  parentRefs:
  - group: ""
    kind: Service
    name: sre-model
    port: 80
  rules:
  - backendRefs:
    - group: enterpriseagentgateway.solo.io
      kind: EnterpriseAgentgatewayBackend
      name: sre-model
