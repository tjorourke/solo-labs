[project]
name = "record-tools"
version = "0.1.0"
description = "MCP server that holds the Diagnosis contract (record_diagnosis)"
readme = "README.md"
requires-python = ">=3.11"
dependencies = [
  "mcp>=1.12.0",
]
