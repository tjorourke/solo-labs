apiVersion: kagent.dev/v1alpha3
kind: AgentTemplate
metadata:
  name: assistant
  namespace: kagent
  labels:
    kagent.dev/harness: kagent          # what the Harness admission selector matches
spec:
  description: A plain assistant running as a substrate actor
  modelConfig:
    name: default-model-config
  systemPrompt: You are a helpful assistant running on kagent.
