kagent-langgraph>=0.9.4
kagent-core>=0.9.4
# kagent-langgraph 0.9.4 imports `a2a.server.apps`, which was removed in
# a2a-sdk 1.0. Pin to the 0.3.x line until upstream catches up.
a2a-sdk>=0.3.23,<1.0
langgraph>=0.2.0
langchain-core>=0.3.0
langchain-anthropic>=0.3.0
langchain-mcp-adapters>=0.2.2
httpx>=0.27.0
uvicorn>=0.30.0
# kagent-core's tracing init unconditionally imports the OpenAI + Google
# GenAI OTel instrumentors, which in turn require the underlying SDK packages
# to be importable — even though we only use Anthropic.
openai>=1.0.0
google-generativeai>=0.8.0
