kagent-langgraph>=0.9.4
kagent-core>=0.9.4
# kagent-langgraph 0.9.4 imports `a2a.server.apps`, removed in a2a-sdk 1.0.
a2a-sdk>=0.3.23,<1.0
langgraph>=0.2.0
langchain-core>=0.3.0
httpx>=0.27.0
uvicorn>=0.30.0
# kagent-core's tracing init unconditionally imports the OpenAI + Google
# GenAI OTel instrumentors, which require the underlying SDKs to be
# importable even though we don't actually use either provider — the agent
# talks to the mock-LLM through the gateway via raw httpx.
openai>=1.0.0
google-generativeai>=0.8.0
# langchain-anthropic is pulled by kagent-langgraph as a soft dep; pin a
# recent compatible version to avoid a resolver surprise on cold install.
langchain-anthropic>=0.3.0
