apiVersion: v1
kind: Service
metadata:
  name: rbac-inspector-ui
  namespace: mcp-rbac
  labels: { app: rbac-inspector-ui }
spec:
  selector: { app: rbac-inspector-ui }
  ports:
    - name: http
      port: 8080
      targetPort: 8080
      appProtocol: http
