# Pin the kagent packages to 0.9.1 to MATCH the enterprise controller (it injects
# cr.kagent.dev/kagent-dev/kagent/app:0.9.1). A newer kagent-langgraph (0.9.7)
# threads the user session to the controller's /api/langgraph/checkpoints callback
# differently, and the 0.4.3 controller rejects it with "no session found" -> 400.
kagent-langgraph==0.9.1
kagent-core==0.9.1
a2a-sdk>=0.3.23,<1.0
# Keep mcp on stable 1.x: --pre (needed for kagent-core's pre-release otel deps)
# would otherwise pull mcp 2.0.0a1, which moved mcp.shared.context.RequestContext
# and breaks langchain-mcp-adapters at import. The langchain 1.x stack
# (langchain-core/openai, langgraph, langchain-mcp-adapters) resolves cleanly on
# its own — only mcp needs the ceiling.
mcp>=1.12,<2
langgraph
langchain-core
langchain-openai
langchain-mcp-adapters
httpx>=0.27.0
uvicorn>=0.30.0
