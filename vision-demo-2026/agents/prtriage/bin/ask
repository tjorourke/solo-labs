#!/usr/bin/env bash
# ask "<question>" — put a question to the hosted agent over kagent's A2A endpoint and
# watch it work. AGENT=releasejava picks the other agent.
exec "$(dirname "$0")/../scripts/ask.sh" "$@"
